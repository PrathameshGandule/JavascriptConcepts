/* Math - built-in method */

console.log(Math.PI)
console.log(Math.E)

let x = 4;
let y = 2;
let z = 1;

z = Math.round(x);    // rounds up if more than 0.5 or down round
z = Math.floor(x);    // rounds down
z = Math.ceil(x);     // rounds up
z = Math.trunc(x);    // removes fractional part
z = Math.pow(x,y);    // power function
z = Math.sqrt(x);     // square root function
z = Math.log(x);      // logarithmic function base e
z = Math.sin(x)       // sin function
z = Math.cos(x);      // cos function
z = Math.tan(x);      // tan function
z = Math.abs(x);      // removes negative sign if any
z = Math.sign(x);     // returns -1 if -ve number 1 if +ve an 0 if 0
let max = Math.max(x, y, z);    // returns max value
let min = Math.min(x, y, z);    // returns min value

console.log(min)

